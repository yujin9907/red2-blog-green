<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.metacoding.red.domain.loves.LovesDao">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into loves(usersId, boardsId, createdAt) values (#{usersId}, #{boardsId}, now())
    </insert>

    <delete id="delete">
        delete from loves where id=#{lovesId};
    </delete>

    <select id="findByDetail"
            resultType="site.metacoding.red.web.response.boards.DetailDto">
        SELECT
            bo.*,
            lo.id lovesId ,
            if(lo.id IS NULL, 0, 1) isLoved,
            (SELECT COUNT(*) FROM loves WHERE boardsId = #{boardsId}) loveCount
        FROM boards bo
                 LEFT OUTER JOIN (SELECT * FROM loves WHERE usersId = #{principalId}) lo
        ON bo.id = lo.boardsId
        WHERE bo.id = #{boardsId}
    </select>


    
    <select id="findByPrimary" resultType="site.metacoding.red.domain.loves.Loves">
        select * from loves where boardsId=#{boardsId} and usersId=#{usersId};
    </select>

</mapper>