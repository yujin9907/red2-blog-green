<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.metacoding.red.domain.loves.LovesDao">

    <insert id="insert">
        insert into loves(usersId, boardsId, createdAt) values (#{usersId}, #{boardsId}, now())
    </insert>

    <delete id="deleteById">
        delete from loves where usersId=#{usersId} and boardsId=#{boardsId}
    </delete>

    <select id="findByUsername" resultType="site.metacoding.red.domain.loves.Loves">
        select count(*) from loves where boardsId=#{boardsId} group by boardsId
    </select>

</mapper>